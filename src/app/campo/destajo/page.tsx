// Tarea por destajo: actividad + precio pactado + unidades + trabajador.
// TODO (Paso 6): el total se calcula solo (precio × unidades). Guardado offline.
export default function DestajoPage() {
  return (
    <div className="tarjeta">
      <h1 className="text-xl font-extrabold mb-2">Tarea por destajo</h1>
      <p className="text-finca-700">
        Precio pactado × unidades ejecutadas. Se construye en el Paso 6.
      </p>
    </div>
  );
}
